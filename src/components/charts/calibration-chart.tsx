'use client'

// v1 stub — Accuracy calibration visualization
// X-axis = predicted probability, Y-axis = actual outcome frequency

export function CalibrationChart() {
  // TODO (v1): Implement calibration chart once events start resolving
  return (
    <div className="flex items-center justify-center h-64 border border-dashed border-slate-700 rounded-lg">
      <p className="text-sm text-slate-500">Calibration chart available after events resolve</p>
    </div>
  )
}
